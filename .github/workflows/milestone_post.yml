# AUTO-GENERATED by scripts/generate_milestones.py
# Do not edit manually. Re-run the script after updating config/semesters.yaml.
#
# Milestone posts at pre-calculated times.

name: Milestone Twitter Post

on:
  schedule:
    - cron: '0 3 15 3 *'  # 2025-2026 bahar dönemi 25%
    - cron: '0 9 11 4 *'  # 2025-2026 bahar dönemi 50%
    - cron: '0 15 8 5 *'  # 2025-2026 bahar dönemi 75%
    - cron: '24 23 24 5 *'  # 2025-2026 bahar dönemi 90%
    - cron: '0 21 4 6 *'  # 2025-2026 bahar dönemi 100%
    - cron: '48 1 8 10 *'  # 2026-2027 güz dönemi 10%
    - cron: '0 9 23 10 *'  # 2026-2027 güz dönemi 25%
    - cron: '0 21 17 11 *'  # 2026-2027 güz dönemi 50%
    - cron: '0 9 13 12 *'  # 2026-2027 güz dönemi 75%
    - cron: '12 16 28 12 *'  # 2026-2027 güz dönemi 90%
    - cron: '0 21 7 1 *'  # 2026-2027 güz dönemi 100%
  workflow_dispatch:
    inputs:
      force:
        description: 'Force milestone post regardless of timing'
        type: boolean
        default: false

jobs:
  post_milestone:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Post milestone tweet
        env:
          API_KEY: ${{ secrets.API_KEY }}
          API_KEY_SECRET: ${{ secrets.API_KEY_SECRET }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          ACCESS_TOKEN_SECRET: ${{ secrets.ACCESS_TOKEN_SECRET }}
          BEARER_TOKEN: ${{ secrets.BEARER_TOKEN }}
        run: python main.py --milestone
